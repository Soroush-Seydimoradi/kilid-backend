server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kilid_db  # آدرس دیتابیس PostgreSQL
    username: postgres                              # نام کاربری دیتابیس
    password: 1234                                # رمز عبور دیتابیس
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # برای ساخت خودکار جداول از روی Entityها (مناسب برای توسعه)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
    open-in-view: false

  sql:
    init:
      mode: always

  # فعال کردن زمان‌بندی، کش یا پروفایل‌ها (در صورت نیاز)
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    root: info
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    org.springframework.web: info

# ⚙️ تنظیمات مربوط به CORS (اختیاری برای Front-end)
cors:
  allowed-origins: "http://localhost:3000"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"

# 🗺️ تنظیمات اختصاصی جغرافیایی (اختیاری)
geo:
  default-radius-meters: 5000